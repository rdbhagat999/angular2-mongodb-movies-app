export class Movie {

	constructor( public title: string, public year: number, public description:string, actors: string[], public poster:string, public hero_image:string, public _id?: string, ) {

	}

}